language: node_js
node_js:
  - "node"
before_script:
  - "npm install --global jasmine"
  - git config --global user.email "travis-ci@ruru.gen.nz"
  - git config --global user.name "Travis CI"
  - git config --global push.default simple
  - npm link
os:
  - linux
  - osx
