'use strict';


describe('core for-free:', () => {
  it('Tests for for-free', () => {
    pending('ToDo');
  });
});


describe('core for-each:', () => {
  it('Tests for for-each', () => {
    pending('ToDo');
  });
});
