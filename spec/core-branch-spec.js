'use strict';


describe('core make-branch:', () => {
  it('Tests for make-branch', () => {
    pending('ToDo');
  });
});


describe('core switch:', () => {
  it('Tests for switch', () => {
    pending('ToDo');
  });
});
